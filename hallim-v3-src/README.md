# Hallim V3 — production-aligned landing design experiment

V3 is independently previewed at `https://sushan5140.github.io/sp-boutique/hallim-v3/`.

**Approved V1 is locked:** branch `hallim-v1-locked-2026-09-23`, `/sp-boutique/hallim/`.

**Approved V2 is locked:** branch `hallim-v2-locked-2026-09-23`, `/sp-boutique/hallim-v2/`.

V3 borrows the ACTUAL canonical brand tokens and UX cues from the production Hallim `app/globals.css` (V4) and `app/hangul/page.module.css`. It is not a Vercel app build or auth replica. Sign-in and existing feature links go to the already-running production app. This standalone static preview is deployed only via the `sp-boutique` GitHub Pages site.

The source `sushan5140/hallium` MUST NOT be modified or pushed to before the user explicitly asks to deploy to Vercel.
